package inc.lilin.crowd.admin.core.test;

import java.util.List;

/**
 * 此class由 MybatisGenerator產生
 * 
 * 請注意：任何修改都會在下一次產生器產生code時被覆蓋
 */
public interface TestMapper {
    int insert(Test record);

    List<Test> selectAll();
}